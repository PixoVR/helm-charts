---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: control-plane-addon-charts
  labels:
    deployment: helm
    tier: platform
  annotations:
    argocd.argoproj.io/sync-wave: "{{- .Values.sync_wave -}}"
spec:
  generators:
  - git:
      repoURL: git@github.com:{{- .Values.github_org -}}/{{- .Values.repo_name -}}.git
      revision: {{ .Values.revision }}
      files:
      - path: {{ .Values.cpl_cluster_addon_charts_filepath }}
  template:
    metadata:
      name: '{{ "{{ path.basename }}" }}'
      labels:
        name: '{{ "{{ path.basename }}" }}'
        deployment: helm
        tier: platform
      annotations:
        argocd.argoproj.io/sync-wave: '{{ "{{ app.syncWave }}" }}'
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: {{ .Values.argocd_cpl_project }}
      source:
        path: '{{ "{{ path }}" }}/chart'
        repoURL: git@github.com:{{- .Values.github_org -}}/{{- .Values.repo_name -}}.git
        targetRevision: {{ .Values.target_revision }}
        helm:
          releaseName: '{{ "{{ path.basename }}" }}'
          ignoreMissingValueFiles: true
          values: |
            parent_folder_id: {{ .Values.parent_folder_id }}
            sa_project_id: {{ .Values.sa_project_id }}
            gke_project_id: {{ .Values.gke_project_id }}
            dns_project_id: {{ .Values.dns_project_id }}
            argocd-image-updater:
              argocd-image-updater:
                serviceAccount:
                  annotations:
                    serviceAccount.annotations.iam.gke.io/gcp-service-account: image-updater@{{- .Values.sa_project_id -}}.iam.gserviceaccount.com
              app_iam: 
                parent_folder_id: {{ .Values.parent_folder_id }}
                sa_project_id: {{ .Values.sa_project_id }}
                gke_project_id: {{ .Values.gke_project_id }}
                app_project_id: {{ .Values.gke_project_id }}
            backstage:
              app_iam: 
                parent_folder_id: {{ .Values.parent_folder_id }}
                sa_project_id: {{ .Values.sa_project_id }}
                gke_project_id: {{ .Values.gke_project_id }}
                app_project_id: {{ .Values.gke_project_id }}
            crossplane:
              crossplane:
                serviceAccount:
                  annotations:
                    serviceAccount.annotations.iam.gke.io/gcp-service-account: crossplane-gcp@{{- .Values.sa_project_id -}}.iam.gserviceaccount.com
              app_iam: 
                parent_folder_id: {{ .Values.parent_folder_id }}
                sa_project_id: {{ .Values.sa_project_id }}
                gke_project_id: {{ .Values.gke_project_id }}
                app_project_id: {{ .Values.gke_project_id }}
            external-secrets:
              external-secrets:
                serviceAccount:
                  annotations:
                    serviceAccount.annotations.iam.gke.io/gcp-service-account: external-secrets@{{- .Values.sa_project_id -}}.iam.gserviceaccount.com
              app_iam: 
                parent_folder_id: {{ .Values.parent_folder_id }}
                sa_project_id: {{ .Values.sa_project_id }}
                gke_project_id: {{ .Values.gke_project_id }}
                app_project_id: {{ .Values.gke_project_id }}
            external-dns:
              external-dns:
                serviceAccount:
                  annotations:
                    serviceAccount.annotations.iam.gke.io/gcp-service-account: external-dns@{{- .Values.sa_project_id -}}.iam.gserviceaccount.com
                extraArgs:
                  - --google-project={{- .Values.dns_project_id }}
                  - --txt-owner-id={{ "{{ nameNormalized }}" }}
              app_iam: 
                parent_folder_id: {{ .Values.parent_folder_id }}
                sa_project_id: {{ .Values.sa_project_id }}
                gke_project_id: {{ .Values.gke_project_id }}
                app_project_id: {{ .Values.dns_project_id }}
              extraArgs:
                - --google-project={{- .Values.dns_project_id }}
                - --txt-owner-id={{ "{{ nameNormalized }}" }}
            infra-admin:
              parent_folder_id: {{ .Values.parent_folder_id }}
              gke_project_id: {{ .Values.gke_project_id }}
      destination:
        server: {{ .Values.cpl_server }}
        namespace: '{{ "{{ app.namespace }}" }}'
      syncPolicy:
        syncOptions:
          - CreateNamespace=true

---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: control-plane-addon-manifests
  labels:
    deployment: helm
    tier: platform
  annotations:
    argocd.argoproj.io/sync-wave: "{{- .Values.sync_wave -}}"
spec:
  generators:
  - git:
      repoURL: git@github.com:{{- .Values.github_org -}}/{{- .Values.repo_name -}}.git
      revision: {{ .Values.revision }}
      files:
      - path: {{ .Values.cpl_cluster_addon_manifests_filepath }}
  template:
    metadata:
      name: '{{ "{{ path.basename }}" }}'
      labels:
        name: '{{ "{{ path.basename }}" }}'
        deployment: kustomize
        tier: platform
      annotations:
        argocd.argoproj.io/sync-wave: '{{ "{{ app.syncWave }}" }}'
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: {{ .Values.argocd_cpl_project }}
      source:
        path: '{{ "{{ path }}" }}/manifests'
        repoURL: git@github.com:{{- .Values.github_org -}}/{{- .Values.repo_name -}}.git
        targetRevision: {{ .Values.target_revision }}
      destination:
        server: {{ .Values.cpl_server }}
        namespace: '{{ "{{ app.namespace }}" }}'
      syncPolicy:
        syncOptions:
          - CreateNamespace=true

